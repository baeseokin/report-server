apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: uploads-pvc
  namespace: report
spec:
  # 위에서 만든 PV의 storageClassName과 동일하게 맞춤 (기존 hostpath 제거)
  storageClassName: manual-d-drive
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 200Gi
